name: player
description: Creative Engineering Podcast Player.
publish_to: "none"
version: 1.0.0+1
environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  podcast_search: ^0.3.1
  cupertino_icons: ^0.1.3
  provider: ^4.0.5
  url_launcher: ^5.4.2
  audioplayers: ^0.15.1-beta
  timeago: ^2.0.26
  flutter_markdown: ^0.3.4
  easy_web_view: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
